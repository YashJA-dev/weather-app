class Endpoints {
  // sample endpoint -
  //! "/login"
  //! if any path params - "/text/{userId}/{key}"
  static const String openApiCompletion =
      "/models/gemini-1.5-flash:generateContent";
}
